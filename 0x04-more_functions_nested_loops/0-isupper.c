#include "main.h"
#include <stdio.h>

/**
 * main - check if letters are uppercase.
 *
 * Return: Always 0.
 */
int main(void)
int _isupper(int c)
{
  if ((c >= 'A') && (c <= 'Z'))
  {
    return (1);
  }
  else
  {
    return (0);
  }
}
